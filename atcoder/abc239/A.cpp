#include <bits/stdc++.h>
using namespace std;
using LL = long long int;

int main() {
    ios::sync_with_stdio(false); cin.tie(0);
    double h; cin >> h;
    cout << fixed << setprecision(10) << sqrt(h * (12800000 + h)) << "\n";
}

