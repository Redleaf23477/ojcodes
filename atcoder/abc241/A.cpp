#include <bits/stdc++.h>
using namespace std;
using LL = long long int;

void solve() {
    vector<int> arr(10);
    for (auto &x : arr) cin >> x;
    int x = 0;
    x = arr[x];
    x = arr[x];
    x = arr[x];
    cout << x << "\n";
}

int main() {
    ios::sync_with_stdio(false); cin.tie(0);
    int T = 1;
    while (T--) {
        solve();
    }
}



